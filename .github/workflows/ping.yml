name: Keep Server Alive

on:
  schedule:
    - cron: "*/10 * * * *"  # every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Curl request
        run: curl -X GET https://gymbud-lwdp.onrender.com/health-check