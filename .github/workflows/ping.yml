name: Keep Server Alive

on:
  workflow_dispatch:
  schedule:
    - cron: "*/10 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render server
        run: curl -f https://gymbud-lwdp.onrender.com/health-check